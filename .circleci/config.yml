version: 2.1
jobs:
  hoge:
    working_directory: ~/workspace
    docker:
      - image: circleci/node:4.8.2
      steps:
        - checkout
        - run:
          name: Echo
          command: echo hello, world.
